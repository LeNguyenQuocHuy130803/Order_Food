spring.application.name=backend_Ecom

## MySQL Database Configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:E-commerce}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:1382003}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

## JWT Configuration  ( m� scret , th?i gian ho?t ??ng c?a acctoken : 30 ph�t , v� time hdong c?a refresh token : 30 ngay )
app.jwt.secret=${JWT_SECRET:MIsMiHz45ATNS6elM6dQLfN6oQIBDSV+KbAc5PE3rlA=}
app.jwt.access-token-expiration=${JWT_ACCESS_EXPIRATION:1800000}  
app.jwt.refresh-token-expiration=${JWT_REFRESH_EXPIRATION:604800000}

## Cloudinary Configuration
## Credentials loaded from:
## 1. System environment variables (highest priority)
## 2. .env.local file (development)
## 3. .env file (shared defaults)
## CloudinaryConfig Bean handles initialization via EnvLoader
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api_key=${CLOUDINARY_API_KEY:}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:}

## File Upload Configuration
app.file.max-size=${FILE_MAX_SIZE:5242880}
app.file.allowed-types=${FILE_ALLOWED_TYPES:image/jpeg,image/png,image/gif,image/webp}
app.upload.folder=${UPLOAD_FOLDER:drinks}
app.upload.max-retries=${UPLOAD_MAX_RETRIES:3}